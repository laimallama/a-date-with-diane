# Existing-source observations

Existing editions are frozen by user instruction; these observations do not authorize edits.

- `x03476` (`gobathroom`, `blad < 675`) and `x03493` (`gobathroom1`, lower-intimacy arm) refer to a skirt without a Saturday alternative. Nearby entries explicitly distinguish the Saturday dress. This is a possible garment-wording inconsistency, pending a focused reachability check. German currently translates the shared wording faithfully. No source logic or existing catalog was changed.
- `x03868` repeats the same skirt wording in `sofatalkb`, surrounded by dress wording. Retained faithfully; existing sources unchanged.
- `x04343` in `search1a` says skirt, although the connected Saturday entries describe a dress. Recorded only; existing wording remains unchanged.

Further garment-wording observations: `x04528` (`bathpee`), `x04562` (`secondplace`),
`x04574`, `x05542`, `x04583`, `x04588`, `x04594` and `x05546` use skirt wording in
potentially shared dress routes. These are not established defects: skirt can mean the
skirt portion of a dress. Reachability/context confirmation remains pending. Existing
source and translations are unchanged; German follows the supplied wording.
